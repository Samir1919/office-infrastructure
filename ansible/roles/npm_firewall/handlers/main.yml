---
- name: Reload systemd configuration
  ansible.builtin.systemd_service:
    daemon_reload: true
  when: not ansible_check_mode

- name: Reload NPM firewall policy
  ansible.builtin.systemd_service:
    name: "{{ npm_firewall_unit_name }}"
    state: reloaded
  when: not ansible_check_mode
