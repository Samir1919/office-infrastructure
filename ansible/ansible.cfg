[defaults]
inventory = inventory/production.yml
vault_password_file = ../scripts/ansible-vault-keychain.sh

roles_path = roles

host_key_checking = True
interpreter_python = auto_silent

forks = 10
timeout = 30
retry_files_enabled = False

[ssh_connection]
pipelining = True